{
  "name": "trade-tracker-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for Option Trade Tracker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
